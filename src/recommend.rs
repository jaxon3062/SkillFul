pub fn default_recommendations() -> Vec<String> {
    vec![
        "Recommendations:".to_string(),
        "1. Improve run_tests once real event data exists.".to_string(),
        "2. Review overlap between repo_search and future search-oriented skills.".to_string(),
        "3. Keep raw prompt capture disabled unless explicitly needed.".to_string(),
    ]
}
